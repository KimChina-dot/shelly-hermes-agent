<?xml version="1.0" encoding="utf-8"?>
<!-- Home-screen widget layout (PHASE 42). Plain LinearLayout/TextViews so
     RemoteViews can render it; the whole card is one tap target. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_home_background"
    android:padding="12dp">

    <TextView
        android:id="@+id/widget_app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Shelly Hermes"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="#FF6B7280" />

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="还没有对话"
        android:textSize="14sp"
        android:textColor="#FF1F2937"
        android:maxLines="2"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/widget_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="开始新的对话"
        android:textSize="12sp"
        android:textColor="#FF6B7280" />
</LinearLayout>
